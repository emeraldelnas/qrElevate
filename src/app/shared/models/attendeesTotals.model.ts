export interface AttendeesTotals {
  totalAttendees: number;
  totalMale: number;
  totalFemale: number;
  totalFirstTimers: number;
  totalMaleFirstTimers: number;
  totalFemaleFirstTimers: number;
}
